# Claude Delivery Framework

This repository holds the **multi-agent delivery workflow** (see `CLAUDE.md`) and `.claude/` agent definitions and templates.

Application source, tests, and tooling were removed so you can scaffold a new codebase from scratch while keeping the same process.
